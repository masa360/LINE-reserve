/**
 * 予約-only版（正式名）
 *
 * 実体コードは現時点で `gas/Code_reservation_only_base.gs` にあります。
 * 今後はこのファイル名（Code_reservation_only.gs）を正式名として扱ってください。
 *
 * 使い方:
 * 1) `gas/Code_reservation_only_base.gs` の全文をコピー
 * 2) GAS の `Code.gs` に貼り付けてデプロイ
 */

